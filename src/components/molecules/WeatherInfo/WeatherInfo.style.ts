import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 10,
  },
  temperature: {
    fontSize: 24,
    fontWeight: "bold",
  },
  condition: {
    fontSize: 16,
    color: "#666",
  },
});